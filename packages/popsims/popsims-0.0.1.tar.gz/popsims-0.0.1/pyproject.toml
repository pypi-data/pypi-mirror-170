
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "popsims"
version = "0.0.1"
dependencies = [
    "astropy",
    "scipy",
    "matplotlib",
    "numpy",
]
