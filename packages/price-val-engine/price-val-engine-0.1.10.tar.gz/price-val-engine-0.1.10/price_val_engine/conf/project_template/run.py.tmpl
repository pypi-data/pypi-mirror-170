import sys
import os

if __name__ == "__main__":
    os.environ.setdefault("PRICE_VAL_ENG_SETTINGS_MODULE", '$project_name.settings')
    try:
        from price_val_engine import cmd
    except ImportError as exc:
        raise ImportError(
            "Couldn't import price_val_engine. Are you sure it's installed!"
        ) from exc
    cmd.run_from_commandline(sys.argv)
