[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "umvlib"
version = "0.0.7"
authors = [
    { name="Ademir Queiroga", email="ademirneto2007@gmail.com" },
]
description = "Umov API sdk"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
]
dependencies = [
    "requests"
]
