[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pydont"
version = "1.0.0"
authors = [
  { name="Jadon Phillips", email="joebiden@kitgaming67.com" },
]
description = "Now you can dont() your if statements!"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]