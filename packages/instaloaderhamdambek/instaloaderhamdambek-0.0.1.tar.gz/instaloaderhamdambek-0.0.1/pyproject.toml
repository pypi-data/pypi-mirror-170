[tool.poetry]
name = "instaloaderhamdambek"
version = "0.0.1"
description = "instaloader"
authors = ["Handam"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
