[tool.poetry]
name = "pygame-utils"
version = "0.3.0"
description = ""
authors = ["Nathan Strong <nathanstrong777@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
pygame = "^2.1.2"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
