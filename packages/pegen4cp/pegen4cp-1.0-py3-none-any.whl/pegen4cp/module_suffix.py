def parse(string: str, rule="start"):
    """Parse a string using the grammar."""
    tokengen = tokenize.generate_tokens(io.StringIO(string).readline)
    tokenizer = Tokenizer(tokengen)
    parser = {class_name}(tokenizer)
    return getattr(parser, rule)()


# @this is the part automatically generated by pegen4cp up to this point
