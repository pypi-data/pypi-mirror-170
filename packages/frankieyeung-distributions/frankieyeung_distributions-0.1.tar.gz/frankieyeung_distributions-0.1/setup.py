from setuptools import setup

setup(name='frankieyeung_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['frankieyeung_distributions'],
      zip_safe=False)
